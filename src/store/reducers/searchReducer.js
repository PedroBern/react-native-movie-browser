export const searchInitialState = {
  search: [],
  totalResults: 0,
  response: false
};

export const searchReducer = (state, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};
